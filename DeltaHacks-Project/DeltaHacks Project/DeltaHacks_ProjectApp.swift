import SwiftUI

// Main entry point of the SwiftUI application
@main
struct DeltaHacks_ProjectApp: App {
    // The body property defines the app's user interface and behavior
    var body: some Scene {
        // WindowGroup is the default container for the app's content in SwiftUI
        WindowGroup {
            // The root view of the app, where the app's UI starts
            ContentView()
        }
    }
}
